// HUDManager.cs — Player overlays & meters (Phase 6 placeholder)
namespace StalkerALifeSandbox.UI.HUD;

/// <summary>
/// Manages on-screen HUD elements: health, stamina, radiation
/// meter, ammo counter, compass, and minimap overlay.
/// Implementation deferred to Phase 6.
/// </summary>
public sealed class HUDManager
{
    public bool IsVisible { get; set; } = true;

    // TODO: Phase 6 — Implement HUD rendering hooks
}
